# Dependencies
node_modules/

# Environment variables (never commit real API keys)
.env
.env.local
backend/.env

# Build output
dist/
build/
frontend/dist/

# Cache (local dev only, avoids re-spending Sectors API credits)
backend/.cache/

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# Office lock files
~$*

# Local design-reference screenshots of third-party sites — never publish
# these to the public repo (copyright/redistribution risk).
Img Referensi/

# --- Berkas kerja: sengaja tidak dipublikasikan ---
# Repo publik dijaga berisi kode aplikasi saja. Dokumen perencanaan, mockup
# acuan, dan catatan kerja tetap ada di komputer pengerja, tidak di GitHub.
docs/
frontend/Mockup/
handoffprompt.md
design-brief.html

# Konfigurasi alat bantu lokal
.claude/

# Boilerplate template Vite, bukan dokumentasi proyek
frontend/README.md
